@section scripts {
<script type="text/javascript">
    $(function(){
        if($('#searchDialogTemplate').length == 1) {
            $('#showSearch').show();
        }
        var db = fruit.databind.root;

            $.extend(db.options, {
                searchClick: function(){
                    var serach = db.getData('serach');
                    db.invoke('setLoadParams', serach);
                },
                showSearchClick: function() {
                    var serachDialog = $('<div></div>').html($('#searchDialogTemplate').html());
                    var serachDialogDB = new fruit.databind(serachDialog[0], {
                        onInit:function(){
                            this.loadData(data);
                            var serach = db.getData('serach');
                            this.loadData({serach:serach});
                        }
                    });
                    serachDialog.dialog({title:'搜索', width:600, height:500, cache:false,modal:true,buttons:[
                        {text:'搜索',handler:function(){
                            var serach = serachDialogDB.getData('serach');
                            db.loadData({serach:serach});
                            db.invoke('setLoadParams', serach);
                            serachDialog.dialog('close');
                        }},
                        {text:'关闭',handler:function(){
                            serachDialog.dialog('close');
                        }},
                    ], onClose:function(){
                        serachDialog.dialog('destroy');
                    }});
                },
                clearClick: function(){
                    db.clearData('serach');
                    db.invoke('setLoadParams', {});
                }
            });
        $.extend(db.options, {
            add: function(){
                fruit.ajax({
                        url:'/api/Business/Fw_Productinfo/Newproid',
                        success:function(data) {
                            parent.showDocTab('', '挂牌管理明细', '/Business/Fw_Productinfo/edit/' + data);
                        }
                    })
            },
            edit: function(){
                    var grid = db.binds.grid;
                    var row = grid.getSelected();
                    if(row == null) {
                        fruit.message('warning', '请先选择一个挂牌管理');
                        return;
                    }
                    parent.showDocTab('icon-house_in', '挂牌管理明细', '/Business/Fw_Productinfo/edit/' + row.proid);
            },
             'delete': function() {
                    var grid = db.binds.grid;
                    var rows = grid.getSelections();
                    if(rows.length == 0) return;
                    var ids = [];
                    $.each(rows, function(){
                        ids.push(this.proid);
                    });
                    $.messager.confirm('确认', '您确认要删除所选记录吗？', function(r){
                        if(r){
                            grid.jq.showLoading('正在处理…');
                            fruit.ajax({
                                method:'DELETE',
                                url: '/api/Business/Fw_Productinfo/'+ids.join(),
                                success: function(data) {
                                    grid.jq.hideLoading();
                                    grid.refresh();
                                }
                            })
                        }
                    });
                },
        });
    });
</script>
}
<div class="z-toolbar">

    <a class="easyui-linkbutton" iconcls="icon-arrow_refresh" plain="true" data-bind="click:'refresh'">刷新</a>
    <a class="easyui-linkbutton" iconcls="icon-add" plain="true" data-bind="click:'add'">新增</a>
    <a class="easyui-linkbutton" iconcls="icon-edit" plain="true" data-bind="click:'edit'">编辑</a>
    <a class="easyui-linkbutton" iconcls="icon-cross" plain="true" data-bind="click:'delete'">删除</a>
    <a class="easyui-splitbutton" plain="true" data-options="iconCls:'icon-download'" suffix="xls" data-bind="click:'download'">导出</a>
</div>
<table id="grid" data-bind="datagrid" data-options="fixSize:{w:4, h:'auto'}, autoRowHeight: false, rownumbers:true, method:'get', url:'/api/business/fw_productinfo', pagination:true, pageSize:20, checkOnSelect:true, selectOnCheck:true, singleSelect:false">
<thead>
<tr>
    <th field="SelectedRowKey" data-options="checkbox:true"></th>
    <th field="proid">商品编号    </th>
    <th field="catid" data-options="editor:{type:'numberbox',options:{required:false}}">分类编号    </th>
    <th field="proname" data-options="editor:{type:'validatebox',options:{required:false}}">品名    </th>
    <th field="spec" data-options="editor:{type:'validatebox',options:{required:false}}">规格    </th>
    <th field="memid" data-options="editor:{type:'validatebox',options:{required:false}}">会员    </th>
    <th field="price" data-options="editor:{type:'validatebox',options:{required:false}}">单价    </th>
    <th field="sellmode" data-options="editor:{type:'validatebox',options:{required:false}}">交易模式    </th>
    <th field="grade" data-options="editor:{type:'validatebox',options:{required:false}}">牌号    </th>
    <th field="minprodcount" data-options="editor:{type:'numberbox',options:{required:false}}">起订量    </th>
    <th field="stock" data-options="editor:{type:'numberbox',options:{required:false}}">库存    </th>
    <th field="isactive" data-options="editor:{type:'numberbox',options:{required:false}}">是否已上架    </th>
    <th field="depositseller" data-options="editor:{type:'numberbox',options:{required:false}}">卖家保证金率%    </th>
    <th field="depositbuyer" data-options="editor:{type:'numberbox',options:{required:false}}">买家保证金率%    </th>
    <th field="issale" data-options="editor:{type:'numberbox',options:{required:false}}">销售（求购）    </th>
    <th field="createdate" data-options="editor:{type:'datebox',options:{required:false},formatter:fruit.formatter.Date}"  formatter="formatter:fruit.formatter.Date">创建日期    </th>
    <th field="modifydate" data-options="editor:{type:'datebox',options:{required:false},formatter:fruit.formatter.Date}"  formatter="formatter:fruit.formatter.Date">修改日期    </th>
    <th field="viewcount" data-options="editor:{type:'numberbox',options:{required:false}}">热度    </th>
    <th field="remark" data-options="editor:{type:'validatebox',options:{required:false}}">备注    </th>
    <th field="procode" data-options="editor:{type:'validatebox',options:{required:false}}">Guid编号    </th>
</tr>
</thead>
</table>
