@section scripts {
    <script type="text/javascript">
        $(function(){
            var db = fruit.databind.root;
            $.extend(db.options, {
                searchClick: function(){
                    var form = db.getData('serach');
                    db.invoke('setLoadParams', form);
                },
                clearClick: function(){
                    db.clearData('serach');
                    db.invoke('setLoadParams', {});
                }
                , onInit: function(){
                }
            });
        });

    </script>
}

<div class="z-toolbar">

    <a class="easyui-linkbutton" iconcls="icon-arrow_refresh" plain="true" data-bind="click:'refresh'">刷新</a>
    <a class="easyui-splitbutton" plain="true" data-options="iconCls:'icon-download'" suffix="xls" data-bind="click:'download'">导出</a>
</div>
<table id="grid" data-bind="datagrid" data-options="'rownumbers':true,'method':'get','pagination':true,'pageSize':20,'holdTotal':true,'fixSize':{'w':4,'h':'auto'},'fitColumns':false,'url':'/api/fa/settle'">
<thead>
<tr>
<th field="CustID" data-options="">合作单位</th>
<th field="FCode" data-options="">业务单号</th>
<th field="Ftype" data-options="">类型</th>
<th field="Date" data-options="">日期</th>
<th field="Amt" data-options="">金额</th>
<th field="Remark" data-options="">摘要</th>
</tr>
</thead>
</table>
