@section scripts {
    <script type="text/javascript">
        $(function(){
            var db = fruit.databind.root;
            $.extend(db.options, {
                searchClick: function(){
                    var form = db.getData('serach');
                    db.invoke('setLoadParams', form);
                },
                clearClick: function(){
                    db.clearData('serach');
                    db.invoke('setLoadParams', {});
                }
                , onInit: function(){
                }
            });
        });

    </script>
}

<div class="z-toolbar">

    <a class="easyui-linkbutton" iconcls="icon-arrow_refresh" plain="true" data-bind="click:'refresh'">刷新</a>
    <a class="easyui-splitbutton" plain="true" data-options="iconCls:'icon-download'" suffix="xls" data-bind="click:'download'">导出</a>
</div>
<table id="grid" data-bind="datagrid" data-options="fixSize:{w:4, h:40}, rownumbers:true, method:'get', url:'/api/mms/ordersale', pagination:true, pageSize:20, holdTotal:true">
<thead>
<tr>
<th field="OrderCode" data-options="">订单编号</th>
<th field="PrmDate" data-options="">下单日期</th>
<th field="UserCode" data-options="">下单人编号</th>
<th field="UserName" data-options="">下单人名称</th>
<th field="PopCode" data-options="">终端编号</th>
<th field="PopName" data-options="">终端名称</th>
<th field="PdtCode" data-options="">产品编号</th>
<th field="PdtName" data-options="">产品名称</th>
<th field="OrdQty" data-options="">订购数量</th>
<th field="OrdPrice" data-options="">单价</th>
<th field="OrdAmt" data-options="">金额</th>
<th field="Remark" data-options="">备注</th>
</tr>
</thead>
</table>
